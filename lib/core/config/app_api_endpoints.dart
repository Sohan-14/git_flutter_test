import 'package:git_flutter_test/core/config/app_constants.dart';

class AppApiEndpoints{
  AppApiEndpoints._();
  
  // auth end points
  static const String login = "${AppConstants.apiBaseUrl}/auth/login";
 
}